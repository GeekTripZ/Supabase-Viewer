# Supabase-Viewer
Supabase viewer для маленьких (жесткий вайбкод)

Не единой строчки кода не было написано мной (код написан нейросетью Opencode Big Peakle)

Для компиляции нужен: Cargo, Rust

cargo build --release
